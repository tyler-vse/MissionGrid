# Optional — Phase 2 provider toggles (defaults keep mock implementations)
# VITE_USE_SUPABASE=true
# VITE_USE_GOOGLE_MAPS=true

# Supabase (user-owned project; never commit real keys)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=

# Google Maps JavaScript API (client key; restrict by HTTP referrer in Google Cloud)
# VITE_GOOGLE_MAPS_API_KEY=
